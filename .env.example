# Backend Environment Variables (.env in /backend)
NODE_ENV=development
PORT=4000

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=clouddesk
POSTGRES_USER=clouddesk
POSTGRES_PASSWORD=clouddesk123

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# CORS
FRONTEND_URL=http://localhost:3000

# Frontend Environment Variables (.env in /frontend)
# VITE_API_URL=http://localhost:4000
# Leave empty for same-origin (when nginx proxies)
